# 環境變數範本檔案
# 複製此檔案為 .env 並填入實際值

# 資料庫連線 URL
# 本地開發環境範例（PostgreSQL）
DATABASE_URL=postgresql://username:password@localhost:5432/storyengine

# 快速測試環境範例（SQLite）
# DATABASE_URL=sqlite:///./story.db

# Render PostgreSQL 範例
# DATABASE_URL=postgresql://username:password@hostname:5432/database_name

# 伺服器埠號（Render 會自動設定 PORT 環境變數）
PORT=8000

# 開發模式
DEBUG=True

# 注意事項：
# 1. 如果遇到 psycopg2-binary 安裝問題，請使用：
#    pip install --only-binary=:all: psycopg2-binary==2.9.10
# 2. 如果只是快速測試，可以使用 SQLite（無需安裝 PostgreSQL）

